export class Pathfinder {
  // EasyStar.js wrapper — implementation comes in NPCs+AI coding card
  findPath(_startX: number, _startY: number, _endX: number, _endY: number): Promise<{x: number; y: number}[]> {
    return Promise.resolve([]);
  }
}
